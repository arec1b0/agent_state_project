apiVersion: v1
kind: Service
metadata:
  name: agent-session-api
  labels:
    app: agent-session
spec:
  type: ClusterIP
  selector:
    app: agent-session
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP